<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.go.bai.eip.aep.evm.dao.AEPEVM006PMapper">		
    <select id="selectMppRsn" parameterType="paramMap" resultType="egovMap">
        /* kr.go.bai.eip.aep.evm.dao.AEPEVM006PMapper.selectMppRsn */
            SELECT MAX(MPP_RSN) AS MPP_RSN
              FROM TBBADDED148M
             WHERE 1=1
           <if test='preAffNo != null and preAffNo != ""'>
               AND PRE_AFF_NO = #{preAffNo}
           </if>
           <if test='affNo != null and affNo != ""'>
               AND AFF_NO = #{affNo}
           </if>
               AND APVR_CD = #{apvrCd}
    </select>
</mapper> 