<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.bai.eip.aep.cam.dao.AEPCAM015PMapper">
    
    <select id="selectMfexPopSttcList" parameterType="paramMap" resultType="egovMap">
    /*kr.go.bai.eip.aep.cam.dao.AEPCAM015PMapper.selectMfexPopSttcList*/
          /* kr.go.bai.eip.bad.fre.dao.AEPCAM015PMapper.selectMfexPopSttcList */
		<![CDATA[
			SELECT RECEV_DY || '-소명-' || GRN_SRNO AS YR_SRNO
					, TO_CHAR(TO_DATE(SMT_DT), 'YYYY-MM-DD') AS SMT_DT
					, PRSR
					, SMT_ORG_NM
					, RECEV_DY
					, GRN_SRNO 
			 FROM TBCSPKDE103M  /* 감사소명자료 기본 */
			WHERE AUD_YR = #{audYr}
			  AND AUD_NO = #{audNo}
		]]>
    </select>

</mapper> 