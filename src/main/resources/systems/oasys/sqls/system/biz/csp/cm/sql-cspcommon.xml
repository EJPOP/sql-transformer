<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="csp/common/001">
	
	<select id="CSPCommonSelectDate" parameterClass="paramMap"
		resultClass="resultMap">
		<![CDATA[
			SELECT 	YYYY AS CD, 
					YYYY AS CD_NM 
			FROM	(
						SELECT TO_CHAR(SYSDATE,'YYYY') - LEVEL + 3 YYYY 
						FROM DUAL 
						CONNECT BY TO_CHAR(SYSDATE,'YYYY') - LEVEL + 3 >= 1980
					)
		]]>
	</select>        
	
	<select id="CSPCommonSelectSmg" parameterClass="paramMap"
		resultClass="resultMap">
		<![CDATA[
			SELECT	SMG_FRT_CVPT_APL_GCC_ORG_CD
					,SMG_CVPT_APL_NO
			FROM	TBCSPDCP101M
			WHERE	REQ_DVSN_CD = #REQ_DVSN_CD#
			AND		ACP_YR = #ACP_YR#
			AND		CVPT_ACP_NO = #CVPT_ACP_NO#
		]]>
		
	</select>
</sqlMap> 
